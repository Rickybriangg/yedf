"""RecoverIQ Automation Layer — standalone minimal scaffold.

This package implements the automated recovery workflow engine described in the
RecoverIQ Automation Layer brief (Milestone 5A). It is a self-contained
standalone build: it scaffolds the minimal Loan / RecoveryCase domain needed to
run, so it does not depend on the wider RecoverIQ system.

Milestone 1 (this delivery): rule engine + nightly re-tiering job.
"""

__version__ = "0.1.0"
